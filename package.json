{
  "name": "pbos",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "pbOS — Personal Branding Operating System (discovery-first, specialized per person)",
  "scripts": {
    "enroll": "node skills/enroll/enroll.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
